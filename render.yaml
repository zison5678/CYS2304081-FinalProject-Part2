services:
  - type: web
    name: cybersecurity-resolution-api
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PYSPARK_PYTHON
        value: python
      - key: PYSPARK_DRIVER_PYTHON
        value: python
